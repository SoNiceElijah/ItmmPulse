const chat = require('./_m_chat');
const user = require('./_m_user');
const message = require('./_m_message');
const team = require('./_m_team');

module.exports = {
    chat,
    user,
    message,
    team
}